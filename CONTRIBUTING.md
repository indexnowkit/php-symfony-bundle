# Contributing

This repository is a read-only split of the `packages/` directory of https://github.com/indexnowkit/php.
Please open issues and pull requests there; changes land here automatically when a release is tagged.

Development happens in the monorepo (Docker-only PHP, `bin/php`, `bin/composer`; see its README and CONTRIBUTING.md).
